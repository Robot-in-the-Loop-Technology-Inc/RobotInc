{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[RobotInc Auto-Onboarding]\nYou see this because the SessionStart hook fired. Absent some other session, do nothing extra: no profile\nread, no sentinel read, no card, no brief, plain session. This session only, follow these steps in order:\n1. Check <config>/.otto-met (CLAUDE_CONFIG_DIR if set, else ~/.claude). Missing or unreadable counts as missing.\n2. Missing -> run roll-call before replying (banner, card, seat question), then stop; skip the rest below.\n3. Present -> do not run roll-call. Read <config>/otto-profile.json; missing or unreadable = defaults\n(balanced verbosity, no seats).\n4. Gate, checked before anything below is drafted: if style.avoid contains session-start-brief, skip step 5\nand go straight to step 6.\n5. Read ./TASKS.md and ./.claude/otto-trace.log in THIS directory only, no fallback to <config>. Up to 3\ndoing lines plus the last 2 trace lines, 5 lines max, no headers. Nothing found or unreadable = show\nnothing here, go to step 6.\n6. If the profile has no seats key and style.declined lacks seat-question, ask the seat question once, in\none line: card and roster stay closed, just the question. A no -> offer to save style.declined with\nseat-question added, get a yes first, then never ask again.\n7. If steps 5 and 6 produced nothing, your entire reply on this topic is exactly this sentence, verbatim,\nnothing before it and nothing after it, never nothing to report or any variant of it: What can I help with?\nIf step 5 or 6 produced real content, show that content, then still close with that same exact sentence.'",
            "timeout": 5
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": [
              "${CLAUDE_PLUGIN_ROOT}/hooks/otto-trace.mjs"
            ],
            "timeout": 5
          }
        ]
      }
    ]
  }
}
